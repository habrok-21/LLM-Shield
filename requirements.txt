# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# Accurate token counting
tiktoken>=0.7.0

# Encoded payload detection uses stdlib only (no extra deps)

# --- ML Guard Model (optional) ---
# Enable by setting GUARD_MODEL_ENABLED=true
# Choose ONE backend:

# A) Lightweight ONNX Runtime backend (~15MB)
# onnxruntime>=1.17.0
# tokenizers>=0.19.0
# numpy>=1.24.0
# requests>=2.31.0

# B) HuggingFace Transformers backend (~200MB+torch)
# transformers>=4.40.0
# torch>=2.0.0
